import React from 'react';
import Button from '../../components/Button';

const ProfileStatusBar = () => (
    <div className="profile-status-bar">
        <Button variant="--text-icons-right--header">
            Войти
        </Button>
    </div>
)

export default ProfileStatusBar